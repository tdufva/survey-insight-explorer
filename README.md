# Survey Insight Explorer

Public GitHub Pages build of the survey insight explorer.

This repository contains the static web app only:

- `index.html`
- `app.js`
- `styles.css`
- `survey-data.js`
- map, icons, manifest, and service worker assets

The raw source workbook and report are intentionally not included in this publish repo.
